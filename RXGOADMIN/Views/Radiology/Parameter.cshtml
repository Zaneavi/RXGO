@model RXGOADMIN.Models.Parameter
@{
    ViewBag.Title = "Category";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="page-breadcrumb">
    <div class="row">
        <div class="col-12 d-flex no-block align-items-center">
            <h4 class="page-title">
                Parameter List
            </h4>
            <div class="ml-auto text-right">
                <nav aria-label="breadcrumb">
                    <ol class="breadcrumb">
                        <li class="breadcrumb-item"><a href="/Admin">Home</a></li>
                        <li class="breadcrumb-item">
                            <a href="javascript:(void);">
                                Parameter
                            </a>
                        </li>
                        <li class="breadcrumb-item active" aria-current="page">
                            Parameter List
                        </li>
                    </ol>
                </nav>
            </div>
        </div>
    </div>
</div>

<div class="container-fluid">
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    @if (TempData["MSG"] != null)
                    {
                        <div class="alert alert-info alert-dismissable">
                            <button type="button" data-dismiss="alert" aria-hidden="true" class="close">×</button>
                            <strong>@TempData["MSG"]</strong>
                        </div>
                    }
                    <button type="button" class="btn btn-success btn-lg" data-toggle="modal" data-target="#myModal">Add Parameter</button>
                </div>
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-12">
            <div class="card">
                <div class="card-body">
                    <div class="table-responsive">
                        <table id="zero_config" class="table table-striped table-bordered">
                            <thead>
                                <tr>
                                    <th>Name</th>
                                    <th>Status</th>
                                    <th>Edit</th>
                                    <th>Delete</th>
                                </tr>
                            </thead>
                            <tbody>
                                @if (ViewBag.CategoryList != null)
                                {
                                    foreach (var i in ViewBag.CategoryList)
                                    {
                                        <tr>
                                            <td>@i.ParameterName</td>
                                            @if (i.IsActive == "True")
                                            {
                                                <td><a href="javascript:void(0)" onclick="ChngStatus('@i.ParameterId','@i.IsActive')" class="btn btn-success btn-xs">ACTIVE</a></td>
                                            }
                                            else
                                            {
                                                <td><a href="javascript:void(0)" onclick="ChngStatus('@i.ParameterId','@i.IsActive')" class="btn btn-danger btn-xs">INACTIVE</a></td>
                                            }
                                            <td><a href="javascript:void(0)" onclick="Edit('@i.ParameterId')">Edit</a></td>
                                            <td>
                                                <a href="javascript:void(0)" onclick="Delete('@i.ParameterId')" class="btn btn-danger btn-xs">
                                                    Delete
                                                </a>
                                            </td>
                                        </tr>
                                    }
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<!-- Modal -->
<div class="modal fade" id="myModal" role="dialog">
    <div class="modal-dialog modal-lg">

        <!-- Modal content-->
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Add Parameter Name</h4>
                <button type="button" class="close" data-dismiss="modal">&times;</button>
            </div>
            <div class="modal-body">
                <div class="card">
                    @using (Html.BeginForm("Parameter", "Radiology", FormMethod.Post, new { @class = "form-sample" }))
                    {
                        <div class="card-body">
                            <div class="row">
                                @Html.HiddenFor(model => model.ParameterId)

                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label>
                                            Parameter Name *
                                        </label>
                                        <div class="input-group">
                                            @Html.TextBoxFor(model => model.ParameterName, new { @placeholder = "Parameter Name", @required = "required", @class = "form-control", @type = "text", @maxlength = "100" })
                                        </div>
                                    </div>
                                </div>

                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label>
                                            Reference Range
                                        </label>
                                        <div class="input-group">
                                            @Html.TextBoxFor(model => model.ReferenceRange, new { @placeholder = "Reference Range", @class = "form-control", @type = "text", @maxlength = "100" })
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label>
                                            Unit
                                        </label>
                                        <div class="input-group">
                                            @Html.DropDownListFor(model => model.UnitId, ViewBag.Categoryinventory as SelectList, new { @class = "form-control req"})
                                        </div>
                                    </div>
                                </div>
                                <div class="col-md-6">
                                    <div class="form-group">
                                        <label>
                                            Description
                                        </label>
                                        <div class="input-group">
                                            @Html.TextAreaFor(model => model.Description, new { @class = "form-control", @type = "text", @maxlength = "100" })
                                        </div>
                                    </div>
                                </div>

                            </div>
                        </div>
                        <div class="border-top">
                            <div class="card-body">
                                <button type="submit" class="btn btn-success">Submit</button>
                            </div>
                        </div>
                    }
                </div>
            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-default" data-dismiss="modal">Close</button>
            </div>
        </div>

    </div>
</div>

<div class="modal fade" id="EditModal" tabindex="-1" role="dialog" aria-labelledby="ModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg">
        <div class="modal-content" id="editpartial">

        </div>
    </div>
</div>

<a data-toggle="modal" data-target="#EditModal" id="editpartial1"></a>

<script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>

<script>
    function ChngStatus(id, status) {
        $.getJSON('/Radiology/ParameterStatus/', { id: id, status: status }, function (data) {
            location.reload();
        });
    }
</script>

<script>
    function Delete(id) {
        var result = confirm("Are you sure want to delete?");
        if (result) {
            $.getJSON('/Radiology/DeleteParameter/', { id: id }, function (data) {
                location.reload();
            });
        }
    }
    function Edit(id) {
        $.ajax({
            cache: false,
            type: 'POST',
            url: '/Radiology/EditParameter',
            data: { id: id },
            success: function (data) {
                $('#editpartial').html('');
                $('#editpartial').html(data);
                $('#editpartial1').trigger('click');
            }
        });
    }
</script>
